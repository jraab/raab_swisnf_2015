#!/bin/bash
 #no reason for this script other than to keep naming of pipelines consistance 
 # i.e. all pipelines are run by using a call.* script, with same name as pipeline.*

 sh pipeline.rnaseq_diffgenes.sh
